#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pioneer/bin/pioneer" "$@"
  ret=$?
else 
  node  "$basedir/../pioneer/bin/pioneer" "$@"
  ret=$?
fi
exit $ret
